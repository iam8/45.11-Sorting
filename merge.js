

/** Merge 2 sorted arrays into a single sorted array. */
function merge(arr1, arr2) {

}


/** Sort the given array of numbers using the merge sort algorithm. */
function mergeSort(array) {

}


module.exports = { merge, mergeSort};
